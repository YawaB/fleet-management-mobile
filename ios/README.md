# iOS native project

This directory is reserved for native iOS sources generated by Expo prebuild.

## Bootstrap

From the project root:

```bash
npx expo prebuild --platform ios --no-install
cd ios && pod install
```

Then run:

```bash
npx expo run:ios
```

## Production helper

A placeholder script location is kept in `ios/scripts/` for iOS production bootstrap automation.
